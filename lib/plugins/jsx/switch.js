const { str } = require('./util')
const { stateExpression } = require('./state')

// make this function parser a bit re-usable

const switcher = (elem, subscription, code) => {
  console.log('ok lets go')
}

module.exports = switcher
