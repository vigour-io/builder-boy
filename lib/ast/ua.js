var uid = 0
const { parseCharIndex } = require('../util')

const parseUa = (node, ua, res, computed) => {
// if (node.type )
  // only include requires if they are defined at the root level of the file
  // node.parent.parent.parent.parent.type === 'program'
  // BinaryExpression
  // IfStatement
  // LogicalExpression
  // ConditionalExpression
  // ObjectExpression
  // Literal
}

exports.parseUa = parseUa
